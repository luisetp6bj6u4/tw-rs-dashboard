# tw-rs-dashboard

台股 RS 動能與族群輪動的每日看板。

- 網頁：`index.html`（純靜態，無外部相依）
- 資料：`data/latest.json`（當日完整資料）、`data/history/<日期>.json`（每日族群與 RRG）
- 更新：由私有 repo `tw-rs-screening` 的 GitHub Actions 每個交易日盤後自動推送

本頁僅為量化統計結果，不構成投資建議。
